import 'package:coachpulse/data/repositories/auth_repository.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('AuthRepository type exists', () {
    expect(AuthRepository, isNotNull);
  });
}
